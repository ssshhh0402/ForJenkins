allprojects {
 apply plugin: 'maven-publish'
 
 publishing {
   publications {
       mavenJava(MavenPublication) {
           from components.java
      }
  }
   repositories {
    maven {
  url 'http://192.168.0.31:8081/repository/dockertesting3/'
  credentials {
           username "$username"
           password "$password"
      }
 }
  }
}
}